<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!--<SwitchPreference-->
        <!--android:defaultValue="true"-->
        <!--android:key="example_switch"-->
        <!--android:summary="@string/pref_description_social_recommendations"-->
        <!--android:title="@string/pref_title_social_recommendations" />-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <Preference
        android:key="pref_static_field_key"
        android:selectable="false"
        android:title="Setting"
        android:persistent="false"
        android:selectAllOnFocus="true"
        android:singleLine="true"
      />

    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="Set Your Password"
        android:inputType="textPassword"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Password" />

    <Preference
        android:key="pref_static_field"
        android:selectable="false"
        android:title="Security Setting"
        android:persistent="false"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        />
    <CheckBoxPreference
        android:title="Screen Lock"
        android:defaultValue="true"
        android:summary="Lock The Screen With Password"
        android:key="checkboxPref" />

    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="Set Update Reminder Frequency"
        android:inputType="text"
        android:key="example_text_reminder"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Reminder For Location" />


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference-->
        <!--android:defaultValue="-1"-->
        <!--android:entries="@array/pref_example_list_titles"-->
        <!--android:entryValues="@array/pref_example_list_values"-->
        <!--android:key="example_list"-->
        <!--android:negativeButtonText="@null"-->
        <!--android:positiveButtonText="@null"-->
        <!--android:title="@string/pref_title_add_friends_to_messages" />-->

</PreferenceScreen>
